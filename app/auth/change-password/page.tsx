import ChangePassword from '@/components/auth/change-password';
import React from 'react';

const page = () => {
    return (
        <div>
            <ChangePassword/>
        </div>
    );
};

export default page;