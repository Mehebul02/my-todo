import TodosPage from '@/components/dashboard/todos/todos';
import React from 'react';

const page = () => {
    return (
        <div>
            <TodosPage/>
        </div>
    );
};

export default page;